interface ProductCategory {
    id: number; // Identificador único de la categoría
    businessId: number; // Referencia al negocio propietario de la categoría
    name: string; // Nombre de la categoría de productos
}
